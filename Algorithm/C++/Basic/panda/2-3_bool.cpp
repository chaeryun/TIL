#include <iostream>

using namespace std;

int main()
{
    // bool : 0(False) 혹은 1(True)
    // bool 은 0 외엔 모든 값을 1로 저장
    bool a = 0;
    bool b = 1;
    bool c = 10;

    // 결과값 : 0 1 1

    cout << a << " " << b << " " << c << endl;

    return 0;
}